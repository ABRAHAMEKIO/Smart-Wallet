{
    "builds": [
      {
        "src": "ui/package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "cd ui && npm install && npm run build"
        }
      }
    ]
  }
  